# Frontend

The frontend is a lightweight HTML/CSS/JS application. It expects the backend FastAPI server to run on `http://127.0.0.1:8000`.

Open `index.html` directly in VS Code using the Live Server extension or any static web server. The page renders Mermaid diagrams from the backend response, displays summaries, and includes a **Zoom in** dropdown to filter diagrams per module.
